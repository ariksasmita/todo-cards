const Wrapper = {
  display: 'flex',
  flexGrow: '2',
}

const Input = {
  flexGrow: '2',
}

const AddButton = {
  height: '100%',
  background: '#fff176',
}

export default {
  Wrapper,
  Input,
  AddButton,
}
